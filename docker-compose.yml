version: "3.8"

services:
  anime-api:
    build: .
    expose:
      - "${PORT}"
    env_file:
      - .env
    restart: unless-stopped

  mongo:
    image: mongo:7
    container_name: mongo
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: example
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    restart: unless-stopped

  mongo-express:
    image: mongo-express:1.0.0
    container_name: mongo-express
    depends_on:
      - mongo
    environment:
      ME_CONFIG_MONGODB_ADMINUSERNAME: root
      ME_CONFIG_MONGODB_ADMINPASSWORD: example
      ME_CONFIG_MONGODB_SERVER: mongo   # 👈 Burada kesin mongo olmalı
      ME_CONFIG_MONGODB_PORT: 27017     # 👈 Port da ekleyelim garanti olsun
    ports:
      - "8081:8081"
    restart: unless-stopped

volumes:
  mongo-data:
